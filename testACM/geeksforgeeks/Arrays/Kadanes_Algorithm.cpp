/* from https://practice.geeksforgeeks.org/problems/kadanes-algorithm/0
 * Given an array containing both negative and positive integers. Find the
 * contiguous sub-array with maximum sum.
 */
/*
 *  solution: https://www.geeksforgeeks.org/largest-sum-contiguous-subarray/
 *  algorithm is little wired, don't find a way to improve it
 */

#include<iostream>

int main(){
    int testCaseNum=0;
    std::cin>>testCaseNum;
    while(--testCaseNum >= 0){
        int arrayLength{0};
        std::cin>>arrayLength;
    }
    for(int i=0; i<testCaseNum; ++i){
    }
    return 0;

}
